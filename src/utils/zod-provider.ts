import { FastifyBaseLogger, FastifyInstance, FastifyTypeProviderDefault, RawServerDefault } from 'fastify';

// Simplified type provider alias for Zod integration
export type ZodTypeProvider = FastifyTypeProviderDefault;
